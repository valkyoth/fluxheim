[server]
listen = []

[stream]
enabled = true

[[stream.routes]]
name = "postgres"
listen = ["127.0.0.1:15432"]
upstreams = ["10.0.0.11:5432", "10.0.0.12:5432"]
# upstream_weights = [1, 2]
# upstream_aliases = ["pg-a", "pg-b"]
# backup_upstreams = []
# drain_upstreams = []
connect_timeout_secs = 5
idle_timeout_secs = 300
proxy_header_timeout_secs = 10
# max_connection_secs = 3600
max_connection_bytes = 1073741824
max_connections = 1024
downstream_proxy_protocol = "off"
trusted_proxies = []
# allow_sources = ["10.0.0.0/8"]
# deny_sources = ["10.0.0.13"]
upstream_proxy_protocol = "off"
upstream_tls = false
# Set true only when hostname upstreams intentionally resolve to private
# addresses through trusted internal DNS. IP-literal upstreams are explicit.
# upstream_dns_allow_private_addresses = true
# upstream_sni = "db.internal.example"
# upstream_verify_cert = true
# upstream_verify_hostname = true
# upstream_ca_path = "/etc/fluxheim/upstreams/db-ca.pem"
# upstream_client_cert_path = "/etc/fluxheim/upstreams/client-chain.pem"
# upstream_client_key_path = "/etc/fluxheim/upstreams/client-key.pem"
