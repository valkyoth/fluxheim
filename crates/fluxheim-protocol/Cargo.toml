[package]
name = "fluxheim-protocol"
version = "1.8.2"
edition = "2024"
rust-version = "1.98.1"
license = "EUPL-1.2"
publish = false
description = "Internal Fluxheim protocol framing helpers."

[dependencies]
sanitization = { version = "2.1.0", features = ["alloc"] }
