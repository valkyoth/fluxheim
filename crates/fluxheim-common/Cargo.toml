[package]
name = "fluxheim-common"
version = "1.8.2"
edition = "2024"
rust-version = "1.98.0"
license = "EUPL-1.2"
publish = false
description = "Shared internal Fluxheim primitives."

[features]
test-support = ["dep:windows-permissions"]

[dependencies]
thiserror = "2.0.20"

[target.'cfg(windows)'.dependencies]
windows-permissions = { version = "0.2.4", optional = true }
